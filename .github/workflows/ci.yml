name: JENIFY CI

on:
  pull_request:
    # Not just [main]: the HQ work is a stack of PRs whose base is another
    # feature branch, so a main-only filter meant every "green" run on those
    # PRs had to be dispatched by hand. Run on every pull request instead.
    branches: ['**']
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: jenify-ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  verify:
    name: Test and build
    runs-on: ubuntu-latest
    timeout-minutes: 25

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm

      - name: Install locked dependencies
        run: npm ci

      - name: Run server tests
        run: npm test

      # Root `npm test` covers @factoryos/server only and root `npm run build`
      # covers shared + server + web only, so until now nothing in CI ever
      # executed or built @factoryos/headquarter. Every HQ green tick was a
      # hand-run local claim. These three steps make CI say what it means.
      - name: Run headquarter tests
        run: npm run test:hq

      - name: Typecheck headquarter
        run: npm run typecheck --workspace @factoryos/headquarter

      - name: Build headquarter site
        run: npm run build:site --workspace @factoryos/headquarter

      # Phase 2, Stage 1. HQ's own HTTP host and the standalone process that
      # boots it. Both are proof that HQ runs without the tenant platform, so
      # both belong in the same pipeline as the server — a split pipeline is
      # exactly how the port contract and its host would drift apart.
      - name: Run hq-host tests
        run: npm run test --workspace @factoryos/hq-host

      - name: Typecheck hq-host
        run: npm run typecheck --workspace @factoryos/hq-host

      - name: Run standalone hq-server tests
        run: npm run test --workspace @factoryos/hq-server

      - name: Typecheck standalone hq-server
        run: npm run typecheck --workspace @factoryos/hq-server

      - name: Build all workspaces
        run: npm run build
